{{define "spec_input"}}## User message: athletic profile and recent activities

The {{ .order }} message you receive will be a JSON object that conforms to the following schema:

```json
{{ .json_schema }}
```

{{end}}